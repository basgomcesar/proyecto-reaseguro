
services:
  backend:
    build:
      context: ./backend   
      dockerfile: Dockerfile 
    ports:
      - "3000:3000"        
    env_file:
      - .env                
  
  frontend:
    build:
      context: ./frontend   
      dockerfile: Dockerfile
    ports:
      - "8080:80"             
